---
version: "3.9"
services:
  dev:
    build:
      context: ../..           # 🔁 Build-Kontext = Projekt-Root
      dockerfile: docker/dev/Dockerfile
    volumes:
      - ../..:/workspace
      - /var/run/docker.sock:/var/run/docker.sock
    working_dir: /workspace
    tty: true
